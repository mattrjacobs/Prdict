{
 "friends" : [{% for friend in friends %}
    { "email" : "{{ friend.email|escape }}",
      "username" : "{{ friend.username|escape }}",
      "key" : "{{ friend.key }}"
    }{% if not forloop.last %},{% endif %} 
 {% endfor %}]
}